---
name: leads-sheet
description: Leah's single Google Sheet for all studio leads ("לידים") - Facebook/Instagram lead-ad leads and website form leads in one list she can open on her phone and mark statuses in. Use whenever asked where leads are, to add leads to the sheet, or to change how leads are collected.
---

# גיליון הלידים

## למה
לאה, 13.9.2026: "אני לא יכולה כל יום להתחיל לרשום בדפים למי להתקשר... עדיין לא ארגנת לי מקום נוח ששם ירדו כל הלידים". מקום אחד, נפתח בטלפון (אפליקציית Google Sheets, חשבון guralea@gmail.com).

## הגיליון
- Spreadsheet `1MfgLcFLpR7ax5hkEerMCOxH-aaaxIi4uMgePlJeEd2o` - נוצר 13.9.2026 בחשבון של לאה (Drive שלה), דרך הכרום.
- https://docs.google.com/spreadsheets/d/1MfgLcFLpR7ax5hkEerMCOxH-aaaxIi4uMgePlJeEd2o/edit
- עמודות: תאריך | שם | טלפון | מקור | טופס | סטטוס | הערות | מזהה. לאה ממלאת סטטוס והערות - הסקריפט אף פעם לא משנה שורה קיימת.

- **שם הגיליון: "לידים 2026 – סטודיו לאה גורא"** (לאה, 13.9: "תכתבי 2026, כי יש לי כמה וכמה טפסים כאלה, שלא יהיה לי בלבול"). כל גיליון או טופס חדש שנוצר בשבילה - עם השנה בשם.
- לאה שלחה גם קישור לגיליון לידים ישן שלה: `1XA5mmscZFA7DUTIlVG5KX7t1iYhNjrzlJdbxnUgs6sQ` (הבוט בלי גישה אליו, 13.9). לא נוגעים בו - הלידים החדשים הולכים לגיליון 2026.

## איך הלידים נכנסים
- **מודעות (פייסבוק + אינסטגרם):** `sync-leads.mjs` (בתיקייה הזו) קורא את כל טפסי הלידים של עמוד הסטודיו `2267713623553786` ב-Graph API ומוסיף רק לידים חדשים (לפי עמודת מזהה `meta:<id>`). הטוקן נשאר ב-`facebook-ads-plan/secrets.json` - לא מוקלד לשום מקום.
- **טפסים באתרים (pilates_leads ב-Firestore):** עוד לא מחובר. חשבון השירות לא יכול לקרוא מ-Firestore (403, אין הרשאת IAM - נבדק 13.9), ואסור לי לשנות הרשאות אבטחה. דרך אפשרית: הטופס באתר שולח עותק לגיליון דרך Apps Script (לאה צריכה לאשר פעם אחת בגוגל).
- **שותף עם הבוט - 13.9.2026 ~12:50**, לאה שיתפה בעצמה (`hagil-sheets-bot@hagil-lo-hasipor.iam.gserviceaccount.com`, עורך). אחרי השיתוף: שם הקובץ "לידים 2026 – סטודיו לאה גורא" (לאה נתנה לו רגע לפני כן את השם "לידיים לסטודיו לפילאטיס 2026" - הוחלף בשם שסוכם), לשונית "לידים", מימין לשמאל, שורת כותרת קפואה, רשימה נפתחת בעמודת סטטוס: חדש / התקשרתי / לא ענתה / נקבע אימון היכרות / לא רלוונטי.
- **הרצה ראשונה 13.9 12:53:** נכנסו 4 שורות - ריקי קשוט (12.9 16:13, ושוב 13.9 09:20), Salwa Kurdi Alago (13.9 06:58), אביבה רום (13.9 10:51, פייסבוק - ליד חדש).
- **Gotcha:** רשימת הטפסים של העמוד (`leadgen_forms`) דורשת Page Access Token - הסקריפט מוציא אותו מטוקן ה-system user בכל ריצה.
- **הפעלה אוטומטית - לאה, 13.9:** "הפעלה אחת... אל תעשי כל פעם שיחה חדשה... אם כל 15 דקות אני אראה שם רשימה ועוד רשימה, לא נצא מזה". כלומר: **אסור להקים משימה מתוזמנת חדשה של Claude לזה** (כל הרצה פותחת שיחה חדשה ברשימה שלה).
  - ניסיון 13.9: משימה של Windows (Task Scheduler) כל 15 דקות, בלי חלון ובלי שיחה - **נחסם על ידי מנגנון ההרשאות של Claude Code**. לא לעקוף. אם לאה רוצה כל 15 דקות - היא צריכה לאשר את זה בהגדרות ההרשאות, או ליצור את המשימה בעצמה.
  - **מה מוגדר בינתיים:** הסנכרון רץ בתחילת המשימה הקיימת `site-health-scan` (כל 4 שעות, בשקט, בלי שיחות חדשות). ניסיון להוסיף אותו גם לדוח 08:08 **נחסם על ידי מנגנון ההרשאות** (13.9) - לא לעקוף, לאה מחליטה.
  - **סגור - לאה, 13.9: כל 4 שעות מספיק.** "אני ממילא לא יכולה לחזור לאנשים כל 15 דקות, חוזרת לאנשים פעם ביום, בבוקר או בערב". לא להציע תדירות גבוהה יותר ולא לחזור לנושא.
  - ידנית: `node .claude/skills/leads-sheet/sync-leads.mjs`. הסקריפט מוסיף רק לידים שעוד לא בגיליון (לפי עמודת מזהה), אז הרצה חוזרת לא יוצרת כפילויות.

## החלטה 13.9.2026 - גיליון אחד, 3 לשוניות, לא פיזיקל
לאה: "כל הלידים — מפייסבוק, מהסיפורים ומ'בת כמה את באמת' — נכנסים לגוגל שיט אחד ולא לפיזיקל. שלוש לשוניות לפי מקור. עמודות: תאריך, שם, טלפון, מקור, סטטוס. תראה לי את הגיליון לאישור."

- **נבנה 13.9 באותו גיליון** (`1MfgLcFLpR7ax5hkEerMCOxH-aaaxIi4uMgePlJeEd2o`), ממתין לאישור לאה:
  - לשונית `פייסבוק` (gid 378512569) - 4 הלידים מהמודעות הועתקו מ"לידים" (סטטוס נשמר).
  - לשונית `סיפורים` (gid 1082426345) - ריקה. המקור: טופס שיתוף סיפור ב-register.html (שם/טלפון/מייל ב-story_contacts). לעמוד הגיל הוא לא הסיפור אין טפסי לידים בפייסבוק (נבדק 13.9).
  - לשונית `בת כמה את באמת` (gid 68009066) - ריקה. המקור: age_test_leads.
  - עמודות A-E: תאריך | שם | טלפון | מקור | סטטוס. עמודה F `מזהה` מוסתרת - נדרשת כדי שהסנכרון לא יכפיל שורות. סטטוס: רשימה נפתחת חדש / התקשרתי / לא ענתה / נקבע אימון היכרות / לא רלוונטי. כותרת בצבע #6E6259, קפואה, מימין לשמאל.
  - הלשונית הישנה `לידים` לא נגעתי בה; `sync-leads.mjs` עדיין כותב אליה עד האישור.
- **אחרי אישור:** לשנות את `sync-leads.mjs` לכתוב ללשונית פייסבוק בפורמט 5+1 עמודות; לחבר את טופס הסיפורים ואת בת כמה דרך Apps Script (`apps-script-webhook.gs` לעדכן ל-3 לשוניות; לאה לוחצת "אישור" בגוגל פעם אחת); הלשונית הישנה - לאה מוחקת בעצמה.
- **לידים מטופס הפילאטיס באתרים** (pilates_leads - guralea.com/pilates.html ו-guraleapilates.com) → לשונית `פייסבוק`, מקור "אתר". לאה אישרה 13.9 ("6 או קי").
- פיזיקל לא מקבל לידים. `pilates-ads-campaign/fizikal-questions.md` נשאר רק כהיסטוריה.

## ביצוע אחרי האישור - 14.9.2026
לאה אישרה 14.9 את הגיליון עם 3 הלשוניות: "ולחבר את שני הטפסים". (מחליף את "sync-leads.mjs עדיין כותב אליה עד האישור" ואת "טפסים באתרים - עוד לא מחובר" למעלה.)

**בוצע ונבדק (מקומית, לא נדחף):**
- `sync-leads.mjs` כותב עכשיו ללשונית `פייסבוק` (`config.json`: `"sheetName": "פייסבוק"`), 6 עמודות: תאריך | שם | טלפון | מקור | סטטוס | מזהה. שורות חדשות נכתבות לשורות הריקות מתחת לשורה האחרונה שיש בה משהו, כך ששורה קיימת לא משתנה (גם השורות שלאה הקלידה בעצמה). כפילויות נמנעות לפי עמודה F (`meta:<id>`). 4 הלידים שהועתקו ב-13.9 נכתבו באותו פורמט, ולכן לא הוכפלו. הלשונית `לידים` כבר לא מתעדכנת, ולאה מוחקת אותה בעצמה.
- **הרצה 14.9 11:14:** נוספה שורה אחת, מאיה פרץ (13.9 22:17, אינסטגרם, `meta:2069625710587723`). הסקריפט קורא את כל טפסי העמוד, כולל הטופס החדש `1032122829849887`. הגיליון נקרא בחזרה: היא בשורה 8, רשימת הסטטוס קיימת בשורה, עמודה F עדיין מוסתרת, ו-2 השורות שלאה הקלידה (לא ברור, נוגה חלילי) לא השתנו. הרצה שנייה מיד אחרי הוסיפה 0 שורות, כלומר אין כפילויות.
- `apps-script-webhook.gs` מנתב ל-3 לשוניות: `story` (register.html) ל-סיפורים, `bat-kama` (bat-kama.html) ל-בת כמה את באמת, `pilates` (guralea.com/pilates.html ו-guraleapilates.com) ל-פייסבוק. אותן 6 עמודות, מקור "אתר", סטטוס "חדש", מזהה `<טופס>:<מזהה המסמך ב-Firestore>`. פתיחת הכתובת בדפדפן (doGet) רק בודקת שהלשוניות קיימות ולא כותבת שורה. אין בקוד סודות.
- **חיבור הטפסים:** העותק לגיליון נשלח רק אחרי שהשמירה ב-Firestore הצליחה. הוא לא מחכה לתשובה, ולא יכול לעצור את השמירה או את הודעת ההצלחה. הכתובת נמצאת במקום אחד, בקובץ החדש `js/leads-sheet.js` (`LEADS_SHEET_WEBAPP_URL`). הקובץ נקרא מ-`js/register.js`, `js/bat-kama.js` ו-`js/pilates-leads.js`. תגיות script נוספו ב-register.html, bat-kama.html ו-pilates.html, והגרסאות הוקפצו: register.js v9, bat-kama.js v5, pilates-leads.js v4, leads-sheet.js v1. נשלחים רק שם וטלפון, בלי מייל ובלי תוצאות המבחן.
- **guraleapilates** (ריפו נפרד, מקומי בלבד): השינוי ב-`src/assets/js/lead-form.js`, ואותו קובץ הועתק ל-`dist/assets/js/lead-form.js`. זה בדיוק מה שה-build עושה לקבצי assets. ה-build המלא לא הורץ, כי `scripts/build.mjs` נמצא באמצע עבודה אחרת (הלוגו).
- עד שמדביקים כתובת אמיתית, הקוד לא שולח כלום (הוא בודק שהכתובת מתחילה ב-`https://script.google.com/macros/s/`).
- לא נכתבה שום שורת בדיקה בגיליון ושום רשומה ב-Firestore.
- הערה: בשתי השורות שלאה הקלידה, "אורגני איסטגרם" נמצא בעמודת סטטוס (E) ולא בעמודת מקור (D). לא נגעתי בזה.

**מה לאה עושה - פעם אחת, במחשב (לא בטלפון):**
1. פותחת את הגיליון "לידים 2026 – סטודיו לאה גורא".
2. בתפריט למעלה: **תוספים** (Extensions) ← **Apps Script**. נפתח חלון חדש.
3. מוחקת את כל מה שכתוב שם, ומדביקה את הקוד ששלחתי לה (הקובץ `apps-script-webhook.gs`, בקובייה אחת להעתקה).
4. לוחצת **שמירה** (או Ctrl+S).
5. למעלה מימין: **פריסה** (Deploy) ← **פריסה חדשה** (New deployment).
6. ליד "בחירת סוג" (Select type) לוחצת על גלגל השיניים ← **אפליקציית אינטרנט** (Web app).
7. **ביצוע בתור** (Execute as): **אני** (Me). **למי יש גישה** (Who has access): **כל אחד** (Anyone).
8. **פריסה** (Deploy) ← **מתן גישה** (Authorize access) ← בוחרת guralea@gmail.com.
9. אם כתוב "Google לא אימתה את האפליקציה": **מתקדם** (Advanced) ← **מעבר אל ... (לא בטוח)**. זה הסקריפט שלה, בגיליון שלה. ואז **אישור** (Allow).
10. מעתיקה את **כתובת אפליקציית האינטרנט** (Web app URL), שמתחילה ב-`https://script.google.com/macros/s/` ונגמרת ב-`/exec`, ושולחת לי בצ'אט.
- אם בעתיד משנים את הקוד: **פריסה ← ניהול פריסות** (Manage deployments) ← עריכה ← גרסה חדשה. **לא** "פריסה חדשה", כי היא נותנת כתובת אחרת והטפסים ימשיכו לשלוח לישנה.

**אחרי שלאה שולחת את הכתובת:**
1. מדביקים אותה ב-`LEADS_SHEET_WEBAPP_URL` ב-`js/leads-sheet.js`. ב-guraleapilates מדביקים ב-`src/assets/js/lead-form.js` ומעתיקים ל-`dist/assets/js/lead-form.js`. אין צורך להקפיץ גרסה שוב, כי הגרסאות החדשות עוד לא באוויר.
2. בודקים בלי שורת בדיקה: פותחים את הכתובת בדפדפן. צריך לחזור `{"ok":true,"tabs":{"סיפורים":true,"בת כמה את באמת":true,"פייסבוק":true}}`.
3. דוחפים ב-Hagil-lo-hasipor: `js/leads-sheet.js`, `js/register.js`, `js/bat-kama.js`, `js/pilates-leads.js`, `register.html`, `bat-kama.html`, `pilates.html`, וגם את התיקייה `.claude/skills/leads-sheet/` (עוד לא ב-git). **שימו לב:** ב-3 קבצי ה-HTML יש גם את שינויי הלוגו, שעוד ממתינים ל"כן" של לאה. לכן דוחפים רק את שורות ה-script, או מחכים לאישור הלוגו.
4. guraleapilates: `src/assets/js/lead-form.js` ו-`dist/assets/js/lead-form.js`. דוחפים רק באישור.

## ✅ פעיל מ-14.9.2026 ~14:36 - הגיליון מושך לידים בעצמו
- **Apps Script** (פרויקט "פרויקט ללא שם", id `1PLQQSv-tgT-xEp-LHiHv5hN-dnJINTteIVJ73uoy2FCnzGFIdAMt9XeZ`, מחובר לגיליון) - הקוד = `apps-script-webhook.gs` (נבדק תו מול תו, hash זהה). הודבק ונשמר על ידי Claude דרך הכרום; לאה לחצה "הרשאת גישה".
- **Web app** גרסה 1 (14.9 13:57, מבצע בתור guralea@gmail.com, גישה: כולם): `https://script.google.com/macros/s/AKfycbxgFHRnAYFq68nK-5l0nOi46WbUiCn0L2QUjaV_U4Z6oZ1w45odyjPzmt2u0dGmRytE/exec` - GET מחזיר `{"ok":true,...,"metaToken":true}` בלי לכתוב כלום.
- **META_TOKEN** - לאה יצרה טוקן System User והדביקה במאפייני הסקריפט בעצמה (Claude לא נגע בטוקן). `setup()` הורץ 14:35:42-14:36:00 בלי שגיאה: נוצר טריגר כל 5 דקות, נוצרה לשונית מוסתרת `_מזהים` עם 5 מזהי הלידים הקיימים, ולשונית פייסבוק לא השתנתה (0 כפילויות, השורות של לאה לא נגעו).
- **שינוי מבנה 14.9:** השורות נכתבות רק ל-A-E. עמודה F שייכת להערות של לאה (ההערות שלה מחקו את המזהים הישנים ב-F). מזהים נשמרים ב-`_מזהים`, וטלפון שכבר קיים בלשונית נחשב "כבר נכנס".
- **שינוי קוד ה-web app:** פריסה ← ניהול פריסות ← עריכה ← גרסה חדשה (לא "פריסה חדשה" - משנה כתובת).
- **טפסי guralea.com מחוברים ובאוויר מ-14.9 ~15:00** (commit a87dc58: `js/leads-sheet.js` עם הכתובת + register.js/bat-kama.js/pilates-leads.js + שורות ה-script בלבד ב-3 ה-HTML - שינויי הלוגו לא נדחפו). נבדק שהדף החי טוען את leads-sheet.js ושהקובץ החי מכיל את הכתובת. לא נשלחה רשומת בדיקה (לאה: בלי רשומות בדיקה) - לבדוק בעיניים כשהליד האמיתי הבא נכנס.
- **נשאר:** guraleapilates.com - `lead-form.js` מעודכן מקומית (src + dist), לא פורסם: `dist/` מכיל עדיין את `admin/` של הפאנל שבוטל, צריך לנקות לפני פרסום. הרצת sync-leads.mjs ב-site-health-scan בוטלה 14.9.

## לידי מודעות ישר לגיליון, בלי Claude - 14.9.2026
לאה, 14.9: **"בקשר ללידים האם את צריכה להכנס כל פעם לבדוק אם יש ליד ואז לשלוח לי? זה לא טוב, אני צריכה שהליד ירד ישר לטופס שייט."**
- **הפתרון:** אותו Apps Script שבגיליון (`apps-script-webhook.gs`) קיבל `syncMetaLeads()` + `setup()`. גוגל מריץ אותו כל 5 דקות בעצמו (טריגר זמן), גם כשהמחשב של לאה כבוי, בלי שיחה חדשה ובלי משימה של Claude. קורא את כל טפסי הלידים של עמוד הסטודיו ומוסיף ללשונית `פייסבוק` רק לידים חדשים (מזהה `meta:<id>`, זהה ל-sync-leads.mjs - אין כפילויות ביניהם).
- **טוקן:** מאפיין סקריפט `META_TOKEN` - טוקן System User נפרד לגיליון (לא הטוקן מ-secrets.json). לאה יוצרת ומדביקה בעצמה (אסור לי להזין טוקן). תוקף: לעולם לא. הרשאות: leads_retrieval, pages_show_list, pages_read_engagement, pages_manage_ads, business_management.
- **ההרצה כל 4 שעות בתוך site-health-scan נשארת כגיבוי** בלבד.
- **צעדים ללאה (פעם אחת, במחשב) - נשלחו 14.9:** (1) Business Settings ← משתמשים ← משתמשי מערכת ← Hagil Lo Hasipur Automation ← יצירת טוקן ← אפליקציה Hagil Lo Hasipur ← תוקף "לעולם לא" ← 5 ההרשאות ← יצירה ← העתקה. (2) בגיליון: תוספים ← Apps Script ← מדביקה את כל הקוד ← שמירה. (3) גלגל שיניים "הגדרות הפרויקט" ← "מאפייני סקריפט" ← הוספה: META_TOKEN = הטוקן ← שמירה. (4) חזרה לעורך ← בחירת הפונקציה setup ← הפעלה ← אישור הרשאות. (5) פריסה ← פריסה חדשה ← אפליקציית אינטרנט ← אני / כל אחד ← פריסה ← שולחת את הכתובת.
- **אחרי שלאה מסיימת:** לבדוק שהטריגר קיים (לאה רואה "טריגרים" בעורך) ושהליד הבא מופיע בלשונית תוך 5 דקות; להדביק את כתובת ה-web app בקוד הטפסים ולדחוף.
5. כשמגיע הליד האמיתי הראשון מכל טופס, פותחים את הלשונית ורואים בעיניים שהשורה נכנסה (כלל 2).

## 16.9.2026 - ליד מהמודעה נבלע בגלל שורה ידנית (תוקן)
- מקרה: לאה הקלידה ידנית את "לאה מושיקוב" 054-940-2335 (הגיעה באורגני). ב-15.9 19:14 אותה אישה מילאה את טופס המודעה (`meta:2051860528786393`). הסקריפט רשם את המזהה ב-`_מזהים` אבל לא הוסיף שורה, כי הטלפון כבר היה בלשונית - ולאה לא ידעה שהמודעה הביאה אותה.
- תיקון (`apps-script-webhook.gs`, `addRows`): טלפון קיים חוסם שורה חדשה רק אם לשורה הקיימת יש "מקור" (D) - כלומר נוצרה אוטומטית. שורה שלאה הקלידה (מקור ריק) לא חוסמת ליד אמיתי.
- הודבק בעורך Apps Script דרך הכרום של לאה (פרויקט `1PLQQSv-tgT-xEp-LHiHv5hN-dnJINTteIVJ73uoy2FCnzGFIdAMt9XeZ`), נשמר ונבדק אחרי רענון. הטריגר `syncMetaLeads` פעיל (הפעלה אחרונה 16.9 12:38, 0% שגיאות). שמירה מספיקה לטריגר; `doPost` (טפסי האתר) ימשיך להריץ את הגרסה הפרוסה הקודמת עד פריסה חדשה.
- השורה של 15.9 נוספה ידנית (שורה 8) ושורה 5 תוקנה ל"לאה מושיאחוב", באישור לאה.
- **לאה לא מדביקה קוד** - כל שינוי בסקריפט נעשה דרך הכרום שלה (ראו זיכרון feedback_claude_performs_all_fixes).

## 21.9.2026 - גרסה 3: מייל לכל ליד + מקור אמיתי מהאתר
- **Web app גרסה 3** (21.9 12:39, אותה כתובת /exec). הקוד בעורך = `apps-script-webhook.gs` (אומת ב-SHA-256 אחרי שמירה ורענון).
- **מייל:** כל שורה חדשה (מודעה או אתר) → `MailApp.sendEmail` ל-guralea@gmail.com, נושא **"ליד חדש: שם"**, גוף: שם/טלפון/מקור/זמן/לשונית. השורה נכתבת לגיליון **לפני** המייל - תקלת מייל לא מאבדת ליד. שמות שמתחילים ב"ניסיון" (בדיקה לילית) נכתבים אבל לא מקבלים מייל. `isTest` (בדיקה/בדיקת טופס/בדיקת מערכת/_healthscan) - לא נכתבים בכלל.
- **יומן `_מיילים`** (לשונית מוסתרת): זמן | מזהה | שם | מקור | "נשלח" / "שגיאה: ...". דוח הבוקר משווה אותו לשורות החדשות ("נכנסו X לידים, נשלחו X מיילים").
- **הרשאת send_mail:** אושרה ע"י לאה 21.9 ~13:00. חלון ההרשאה של גוגל נפתח כחלון Edge נפרד שהתוסף לא רואה - לאה לוחצת בו בעצמה. בפעם הראשונה אושרה הרשאה חלקית בלי שליחת מייל (שגיאה ביומן 12:41) - אם זה חוזר, מריצים פונקציה שקוראת ל-MailApp כדי לבקש שוב.
- **מקור:** guraleapilates.com (`site.js`) שומר בדף הראשון של הביקור מאיפה הגיעה הגולשת (referrer/utm) → `lead-form.js` שולח `source`. ערכים מותרים: `SITE_SOURCES` - פוסט/מודעה - פייסבוק/אינסטגרם/טיקטוק · גוגל · אתר הקהילה · אתר - ישיר. guralea.com/pilates.html ממשיך לשלוח "אתר".
- **אומת מקצה לקצה 21.9 13:06:** פוסט פייסבוק של הסטודיו → קישור → טופס → שורה "פוסט - פייסבוק" → מייל בדואר הנכנס 13:06. שורות הבדיקה נמחקו.
- **עורך Apps Script:** תפריט בחירת הפונקציה לא מגיב ללחיצות התוסף; העורך מריץ את הפונקציה הראשונה בקובץ.

## 21.9.2026 - גרסה 4: לשונית "סדנה"
- לשונית **"סדנה"** נוצרה (שכפול של "פייסבוק" בלי לידים: אותן עמודות, RTL, רשימת הסטטוסים). form `workshop` (guralea.com/workshop.html) ו-`bat-kama-workshop` (מקור "מבחן") → סדנה.
- מייל לסדנה: נושא **"ליד חדש לסדנה: שם"** (`MAIL_SUBJECTS`).
- guralea.com: `js/leads-sheet.js` v3 שומר את מקור הביקור בדף הראשון ושולח אותו **רק עם לידי הסדנה** (שאר הטפסים נשארים "אתר"). `workshop.html` טוען את leads-sheet.js; `workshop-leads.js` v8 שולח לגיליון אחרי השמירה ב-Firestore.
- סריקה לפני החיבור: workshop_leads - 8 מסמכים, **כולם בדיקות**; age_test_leads - 0; contact_submissions - 7 בדיקות. **אין הרשמות אמיתיות שאבדו.**
- **אומת 21.9 14:47:** workshop.html?utm_source=facebook&utm_medium=organic → טופס → שורה בסדנה "פוסט - פייסבוק" → מייל "ליד חדש לסדנה: ..." בדואר הנכנס 14:47. הבדיקה נמחקה מהגיליון, מ-_מיילים ומ-Firestore.
- Firestore נקרא רק דרך `db` בדף guralea.com/admin/dashboard.html ב-Edge של לאה (חשבון השירות מקבל 403).
