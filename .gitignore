# Facebook API credentials for the facebook-teaser skill - never commit these to this public repo
.claude/skills/facebook-teaser/secrets.json

# Metricool API credentials - never commit these to this public repo
.claude/skills/facebook-teaser/metricool-secrets.json

# WhatsApp Business number/credentials for story-reward-vouchers - never commit, this repo is public
.claude/skills/story-reward-vouchers/secrets.json

# Google Cloud service-account key (Sheets/Drive/Calendar API) - never commit, this repo is public
.claude/skills/google-account-access/service-account-key.json

# Meta Ads Manager access token (ads_management/ads_read) - never commit, this repo is public
.claude/skills/facebook-ads-plan/secrets.json

# Local testimonial selection page (working files, never commit)
_bchira-local/

# 21.9.2026: published paper kept locally only (public repo)
.claude/skills/bat-kama-at-beemet/prototype-archive-2026-09-20/tveter2014.*
