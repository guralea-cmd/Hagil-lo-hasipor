# יומן ממצאים - סריקת תקינות האתר

עוקב אחרי כל בדיקה שמצאה בעיה (גם חד-פעמית/חולפת), כדי להבחין בין תקלה אמיתית שחוזרת לבין "פלוק" חד-פעמי, בלי לפתוח כל פעם דיון מחדש.

| תאריך ושעה | עמוד/בדיקה | מה נמצא | פעולה |
|---|---|---|---|
| 2026-08-13/14 (רטרואקטיבי) | register.html - העלאת תמונה | ניסיון ראשון נכשל (storage/unauthorized), ניסיון שני (דקות אחר כך) הצליח - כנראה פלוק חד-פעמי, לא אומת כתקלה חוזרת | לא תוקן דבר, תשתית אושרה כתקינה |
| 2026-08-16 | כל 8 העמודים - קונסול | שגיאת "firebase.storage is not a function" שנראתה בטאב ישן התבררה כשארית מבאפר קונסול של טאב קודם (טעינה מ-v=3), לא שגיאה אמיתית - בטאב נקי לגמרי אין שגיאה. התיקון guard (`typeof firebase.storage === "function"`) שכבר הוכנס ל-`js/firebase-config.js` בקומיט `5a80753` (מריצה קודמת של הסריקה) פעיל בפועל באתר החי, `firebase-config.js?v=4`, ומאושר ללא שגיאות | לא בוצעה פעולה - התיקון הקודם עובד כמצופה |
| 2026-08-16 | index.html, blog.html, about.html, events.html, advertise.html, stories.html - שאילתת `ad_submissions` (באנרים) | עדיין חסר אינדקס Firestore מורכב (`status`+`page`+`createdAt`) - שגיאת `failed-precondition` בקונסול בכל עמוד עם banner-slot. זהו סעיף ידוע ומתועד כבר (site-open-items #1, מ-2026-08-14), לא דחוף כי אין עדיין באנרים מאושרים | לא בוצעה פעולה - פריט פתוח ידוע, לא ממצא חדש |
| 2026-08-16 | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ בדיקה אמיתי (PNG זעיר) לנתיב `story_submissions/_healthscan_<ts>/test.png` - ההעלאה הצליחה (uploaded + getDownloadURL תקין). ניסיון מחיקה לאחר מכן נכשל כצפוי (`storage/unauthorized`) - כללי האבטחה חוסמים מחיקה מצד הלקוח הציבורי, בדיוק כמו שצריך. קובץ הבדיקה הזניח נשאר בבאקט (לא נראה בשום מקום באתר, אין רפרנס אליו ב-Firestore) | לא בוצעה פעולה - נתיב ההעלאה תקין ועובד |
| 2026-08-16 | register.html - בדיקת כתיבה אמיתית ל-Firestore db | ניסיון כתיבה עם ערך `status` לא תקני נחסם כראוי ע"י חוקי Firestore (`permission-denied`) - מעיד שהחוקים אוכפים ולידציה כמצופה. ניסיון שני עם `status:"pending"` (התואם בדיוק לזרימה האמיתית) נחסם ע"י ה-classifier של סביבת הריצה, כדי למנוע יצירת רשומת "סיפור ממתין" מזויפת בתור אמיתית - הבדיקה המלאה של נתיב ה-db לא הושלמה הפעם מסיבה זו (לא כשל אמיתי) | לא בוצעה פעולה - לשקול בהרצה הבאה נתיב בדיקה שלא מדמה סטטוס "pending" אמיתי |
| 2026-08-14 (הרצה ידנית ראשונה) | index.html, stories.html - שגיאת קונסולה | `firebase.storage is not a function` - נטען firebase-config.js בלי firebase-storage-compat.js, נשחזר פעמיים | תוקן ופורסם (commit 5a80753), אותו תבנית הגנה כמו firebase.auth() |
| 2026-08-14 (אותה הרצה) | index.html, stories.html - שגיאת קונסולה | `permission-denied` מ-Firestore - כנראה js/banner-display.js שמנסה לקרוא ad_submissions לפני שחוקי Firestore המעודכנים הודבקו בקונסולה (פריט פתוח מוכר, לא חדש) | לא תוקן - זו לא קטגוריה בטוחה לתיקון אוטומטי (חוקי Firestore), וכבר מתועד ברשימת המשימות הפתוחות |
| 2026-08-18 | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). השגיאה היחידה שחזרה בכל עמוד עם banner-slot היא אותה שגיאת `failed-precondition` (חסר אינדקס Firestore מורכב ל-`ad_submissions`: status+page+createdAt) - נבדקה פעמיים (טעינה חוזרת ל-index.html), שחזרה בעקביות, זהה לממצא הידוע מ-2026-08-14/08-16 | לא בוצעה פעולה - פריט פתוח ידוע וקיים, לא ממצא חדש |
| 2026-08-18 | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה (js/stories.js) החזירה תוכן תקין (סיפורים + עימוד 1-4). נבדקו 3 כתובות תמונה מ-Firebase Storage של הסיפור הראשון (photoUrls) - כולן נטענו בהצלחה (`complete:true`, `naturalWidth>0`) | לא בוצעה פעולה - תקין |
| 2026-08-18 | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי לנתיב `story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה וה-URL להורדה תקין. מאשר שנתיב ההעלאה שנכשל בפלוק החד-פעמי ב-2026-08-13 עדיין עובד | לא בוצעה פעולה - תקין |
| 2026-08-18 | register.html - בדיקת כתיבה אמיתית ל-Firestore db | נחסם ע"י ה-classifier של סביבת הריצה (גם ניסיון עם status לא תקני הפעם, לא רק status:"pending") - בדיקת נתיב ה-db לא הושלמה שוב, זו מגבלת סביבת הריצה ולא ממצא על האתר עצמו | לא בוצעה פעולה - לשקול בהרצה הבאה גישה אחרת לבדיקת נתיב ה-db (למשל דרך admin SDK בסביבה נפרדת) אם רוצים כיסוי מלא |
