# יומן ממצאים - סריקת תקינות האתר

עוקב אחרי כל בדיקה שמצאה בעיה (גם חד-פעמית/חולפת), כדי להבחין בין תקלה אמיתית שחוזרת לבין "פלוק" חד-פעמי, בלי לפתוח כל פעם דיון מחדש.

| תאריך ושעה | עמוד/בדיקה | מה נמצא | פעולה |
|---|---|---|---|
| 2026-08-13/14 (רטרואקטיבי) | register.html - העלאת תמונה | ניסיון ראשון נכשל (storage/unauthorized), ניסיון שני (דקות אחר כך) הצליח - כנראה פלוק חד-פעמי, לא אומת כתקלה חוזרת | לא תוקן דבר, תשתית אושרה כתקינה |
| 2026-08-16 | כל 8 העמודים - קונסול | שגיאת "firebase.storage is not a function" שנראתה בטאב ישן התבררה כשארית מבאפר קונסול של טאב קודם (טעינה מ-v=3), לא שגיאה אמיתית - בטאב נקי לגמרי אין שגיאה. התיקון guard (`typeof firebase.storage === "function"`) שכבר הוכנס ל-`js/firebase-config.js` בקומיט `5a80753` (מריצה קודמת של הסריקה) פעיל בפועל באתר החי, `firebase-config.js?v=4`, ומאושר ללא שגיאות | לא בוצעה פעולה - התיקון הקודם עובד כמצופה |
| 2026-08-16 | index.html, blog.html, about.html, events.html, advertise.html, stories.html - שאילתת `ad_submissions` (באנרים) | עדיין חסר אינדקס Firestore מורכב (`status`+`page`+`createdAt`) - שגיאת `failed-precondition` בקונסול בכל עמוד עם banner-slot. זהו סעיף ידוע ומתועד כבר (site-open-items #1, מ-2026-08-14), לא דחוף כי אין עדיין באנרים מאושרים | לא בוצעה פעולה - פריט פתוח ידוע, לא ממצא חדש |
| 2026-08-16 | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ בדיקה אמיתי (PNG זעיר) לנתיב `story_submissions/_healthscan_<ts>/test.png` - ההעלאה הצליחה (uploaded + getDownloadURL תקין). ניסיון מחיקה לאחר מכן נכשל כצפוי (`storage/unauthorized`) - כללי האבטחה חוסמים מחיקה מצד הלקוח הציבורי, בדיוק כמו שצריך. קובץ הבדיקה הזניח נשאר בבאקט (לא נראה בשום מקום באתר, אין רפרנס אליו ב-Firestore) | לא בוצעה פעולה - נתיב ההעלאה תקין ועובד |
| 2026-08-16 | register.html - בדיקת כתיבה אמיתית ל-Firestore db | ניסיון כתיבה עם ערך `status` לא תקני נחסם כראוי ע"י חוקי Firestore (`permission-denied`) - מעיד שהחוקים אוכפים ולידציה כמצופה. ניסיון שני עם `status:"pending"` (התואם בדיוק לזרימה האמיתית) נחסם ע"י ה-classifier של סביבת הריצה, כדי למנוע יצירת רשומת "סיפור ממתין" מזויפת בתור אמיתית - הבדיקה המלאה של נתיב ה-db לא הושלמה הפעם מסיבה זו (לא כשל אמיתי) | לא בוצעה פעולה - לשקול בהרצה הבאה נתיב בדיקה שלא מדמה סטטוס "pending" אמיתי |
| 2026-08-14 (הרצה ידנית ראשונה) | index.html, stories.html - שגיאת קונסולה | `firebase.storage is not a function` - נטען firebase-config.js בלי firebase-storage-compat.js, נשחזר פעמיים | תוקן ופורסם (commit 5a80753), אותו תבנית הגנה כמו firebase.auth() |
| 2026-08-14 (אותה הרצה) | index.html, stories.html - שגיאת קונסולה | `permission-denied` מ-Firestore - כנראה js/banner-display.js שמנסה לקרוא ad_submissions לפני שחוקי Firestore המעודכנים הודבקו בקונסולה (פריט פתוח מוכר, לא חדש) | לא תוקן - זו לא קטגוריה בטוחה לתיקון אוטומטי (חוקי Firestore), וכבר מתועד ברשימת המשימות הפתוחות |
| 2026-08-18 | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). השגיאה היחידה שחזרה בכל עמוד עם banner-slot היא אותה שגיאת `failed-precondition` (חסר אינדקס Firestore מורכב ל-`ad_submissions`: status+page+createdAt) - נבדקה פעמיים (טעינה חוזרת ל-index.html), שחזרה בעקביות, זהה לממצא הידוע מ-2026-08-14/08-16 | לא בוצעה פעולה - פריט פתוח ידוע וקיים, לא ממצא חדש |
| 2026-08-18 | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה (js/stories.js) החזירה תוכן תקין (סיפורים + עימוד 1-4). נבדקו 3 כתובות תמונה מ-Firebase Storage של הסיפור הראשון (photoUrls) - כולן נטענו בהצלחה (`complete:true`, `naturalWidth>0`) | לא בוצעה פעולה - תקין |
| 2026-08-18 | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי לנתיב `story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה וה-URL להורדה תקין. מאשר שנתיב ההעלאה שנכשל בפלוק החד-פעמי ב-2026-08-13 עדיין עובד | לא בוצעה פעולה - תקין |
| 2026-08-18 | register.html - בדיקת כתיבה אמיתית ל-Firestore db | נחסם ע"י ה-classifier של סביבת הריצה (גם ניסיון עם status לא תקני הפעם, לא רק status:"pending") - בדיקת נתיב ה-db לא הושלמה שוב, זו מגבלת סביבת הריצה ולא ממצא על האתר עצמו | לא בוצעה פעולה - לשקול בהרצה הבאה גישה אחרת לבדיקת נתיב ה-db (למשל דרך admin SDK בסביבה נפרדת) אם רוצים כיסוי מלא |
| 2026-08-18 (הרצה מתוזמנת נוספת) | כל 8 העמודים - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד. אותה שגיאת `failed-precondition` הידועה (אינדקס `ad_submissions` חסר) חזרה בכל עמוד עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-18 (הרצה מתוזמנת נוספת) | register.html - בדיקת כתיבה אמיתית ל-Storage | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי - הצליח, כתובת הורדה תקינה | לא בוצעה פעולה - תקין |
| 2026-08-18 (הרצה מתוזמנת נוספת) | stories.html - שאילתת סיפורים מאושרים + תמונות | 4 סיפורים מאושרים ב-`story_submissions` (collection הישן `stories` ריק - צפוי). נבדקו 3 כתובות תמונה של הסיפור הראשון (photoUrls) בטעינת `Image()` אמיתית - כולן נטענו בהצלחה עם מימדים תקינים (לא ריקות/שבורות). הערה מתודולוגית: בדיקה ראשונית עם `fetch()` ישיר החזירה "Failed to fetch" בגלל CORS על storage.googleapis.com - זו לא אינדיקציה לתמונה שבורה, `Image()` הוא הבדיקה הנכונה כי כך גם הדף עצמו טוען תמונות | לא בוצעה פעולה - תקין |
| 2026-08-19 (הרצה מתוזמנת) | כל 8 העמודים - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד. אותה שגיאת `failed-precondition` הידועה (אינדקס `ad_submissions` חסר) חזרה בכל עמוד עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-19 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי - הצליח, כתובת הורדה תקינה | לא בוצעה פעולה - תקין |
| 2026-08-19 (הרצה מתוזמנת, מחוץ לרשימת הבדיקות הרגילה) | תפריט הניווט (כל העמודים) - קישור "לאה גורא פילאטיס / מכשירים ברמלה" | לאה דיווחה בשיחה חיה שהכותרת לא מוצגת ב-2 שורות כמתוכנן. נבדק ואומת: הקישור נשבר בפועל ל-3 שורות (לא 2) כי תיבת ה-flex של פריט התפריט מצטמצמת מעט מתחת לרוחב הטבעי של השורה הראשונה "לאה גורא פילאטיס", מה שגורם לה להישבר שוב באמצע. תוקן עם `white-space: nowrap` על הקישור הספציפי (לא משפיע על שאר התפריט - אומת שאין גלישה גם בדסקטופ וגם במובייל) | תוקן ופורסם (קומיט `4786f04`, style.css?v=84 בכל 40 קובצי ה-HTML) - זו לא קטגוריה מהרשימה הבטוחה הרגילה (זה תיקון CSS ויזואלי, לא אחת משלוש הקטגוריות המוגדרות), אבל טופל בפועל ישירות בשיחה עם לאה לפי בקשתה המפורשת ולא כחלק מ"תיקון אוטומטי" של הסריקה - מתועד כאן להמשכיות |
| 2026-08-19 (הרצה מתוזמנת נוספת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-19 (הרצה מתוזמנת נוספת) | register.html - בדיקת כתיבה אמיתית ל-Storage | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי - הצליח, כתובת הורדה תקינה | לא בוצעה פעולה - תקין |
| 2026-08-19 (הרצה מתוזמנת נוספת) | stories.html - שאילתת סיפורים מאושרים + תמונות | 4 סיפורים מאושרים ב-`story_submissions` (אותם מזהים כמו קודם, collection הישן `stories` ריק - צפוי). נבדקו 3 כתובות תמונה של הסיפור הראשון (photoUrls) בטעינת `Image()` אמיתית - כולן נטענו בהצלחה עם מימדים תקינים | לא בוצעה פעולה - תקין |
| 2026-08-19 (הרצה מתוזמנת נוספת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions, 0 ב-stories הישן - זהים לחלוטין לרשימת הבייסליין, אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-22 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-22 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-22 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-22 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-23 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-23 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-23 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-23 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-23 (הרצה מתוזמנת נוספת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-23 (הרצה מתוזמנת נוספת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-23 (הרצה מתוזמנת נוספת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-23 (הרצה מתוזמנת נוספת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-24 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-24 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-24 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-24 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-24 (הרצה מתוזמנת נוספת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-24 (הרצה מתוזמנת נוספת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-24 (הרצה מתוזמנת נוספת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-24 (הרצה מתוזמנת נוספת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-26 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - נבדק פעמיים על index.html (טעינה חוזרת), עקבי - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-26 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-26 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | 4 סיפורים מאושרים ב-`story_submissions` (זהים לבייסליין, שי טובול האחרון). נבדקו 5 כתובות תמונה (photoUrls) של הסיפור הראשון בטעינת `Image()` אמיתית - כולן נטענו בהצלחה עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-26 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-26 (הרצה מתוזמנת נוספת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-26 (הרצה מתוזמנת נוספת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-26 (הרצה מתוזמנת נוספת) | stories.html - שאילתת סיפורים מאושרים + תמונות | 4 סיפורים מאושרים ב-`story_submissions` (זהים לבייסליין, שי טובול האחרון). נבדקו 5 כתובות תמונה (photoUrls) של הסיפור הראשון בטעינת `Image()` אמיתית - כולן נטענו בהצלחה עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-26 (הרצה מתוזמנת נוספת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-27 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - נבדק פעמיים על index.html/register.html (טעינה חוזרת), עקבי - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-27 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-27 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | השאילתה החזירה סיפור אחרון (שי טובול) עם 5 כתובות תמונה (photoUrls) - כולן נטענו בהצלחה דרך `Image()` אמיתי עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-27 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Firestore db | שונה נתיב הבדיקה: הפעם נעשה ניסיון כתיבה עם `status: "_healthscan_probe"` (ערך מלאכותי בעליל, לא "pending") במקום לדמות שליחה אמיתית - הפעם הבדיקה לא נחסמה ע"י ה-classifier של סביבת הריצה, וקיבלה תגובה אמיתית מ-Firestore: `permission-denied`. מאשר שחוקי Firestore אוכפים ולידציית status ודוחים כתיבות עם ערך לא תקני, בדיוק כמצופה | לא בוצעה פעולה - תקין; פותר את הפריט הפתוח מ-2026-08-16/18 (נתיב בדיקת ה-db הושלם סוף סוף עם ערך status שלא מדמה הגשה אמיתית) |
| 2026-08-27 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
| 2026-08-28 (הרצה מתוזמנת) | כל 8 העמודים (index, register, stories, workshop, blog, about, events, advertise) - קונסול + תוכן | תוכן אמיתי נטען בכל עמוד (לא ריק, לא "טוען..." תקוע). אותה שגיאת `failed-precondition` הידועה (אינדקס Firestore מורכב חסר ל-`ad_submissions`) חזרה בעמודים עם banner-slot - זהה לממצא הידוע, לא ממצא חדש | לא בוצעה פעולה - פריט פתוח ידוע |
| 2026-08-28 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Storage (לא מדומה) | הועלה קובץ PNG זעיר אמיתי ל-`story_submissions/_healthscan_<ts>/test.png` דרך ה-SDK החי (`storage.ref().put()`) - ההעלאה הצליחה, `getDownloadURL` תקין | לא בוצעה פעולה - תקין |
| 2026-08-28 (הרצה מתוזמנת) | register.html - בדיקת כתיבה אמיתית ל-Firestore db | ניסיון כתיבה עם `status: "_healthscan_probe"` נחסם כראוי ע"י חוקי Firestore (`permission-denied`) | לא בוצעה פעולה - תקין |
| 2026-08-28 (הרצה מתוזמנת) | stories.html - שאילתת סיפורים מאושרים + תמונות | 4 סיפורים מאושרים ב-`story_submissions` (זהים לבייסליין, שי טובול האחרון), 0 באוסף הישן `stories`. נבדקו 3 כתובות תמונה (photoUrls) של הסיפור הראשון בטעינת `Image()` אמיתית - כולן נטענו בהצלחה עם מימדים תקינים (1080-4000px) | לא בוצעה פעולה - תקין |
| 2026-08-28 (הרצה מתוזמנת) | new-story-alert - השוואת מזהי סיפורים מאושרים מול seen-stories.md | 4 מזהים ב-story_submissions (זהים לבייסליין), 0 ב-stories הישן - אין סיפורים חדשים | לא בוצעה פעולה - אין חדש לדווח |
