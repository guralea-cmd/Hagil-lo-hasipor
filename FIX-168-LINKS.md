# תיקון 168 קישורים שבורים בעמוד הפייסבוק של הסטודיו

**נכתב:** 19.9.2026
**נכתב על ידי:** סשן Claude Code בענן — שלא יכול לבצע את המשימה בעצמו (ראה "למה הקובץ הזה קיים")
**מיועד ל:** סשן Claude Code שרץ **מהמחשב של לאה**, עם Claude in Chrome מחובר

---

## למה הקובץ הזה קיים

הקישור `tc-leads.co.il` מופיע ב-168 פוסטים בעמוד הפייסבוק של הסטודיו ומחזיר 404.
צריך להחליף אותו ב-`https://guralea.com/pilates.html#form`.

**הדומיין `tc-leads.co.il` שייך לספק לידים חיצוני, לא ללאה** — לכן הפתרון הנקי (הפניית 301 מהדומיין הישן, שהייתה מתקנת את כל 168 הפוסטים במכה אחת וגם כל שיתוף בוואטסאפ/מייל) **לא אפשרי**. חייבים ללכת פוסט-פוסט.

המשימה דורשת דפדפן מחובר לחשבון הפייסבוק של לאה. סשן ענן לא יכול — הוא רץ בקונטיינר מבודד בלי הסשן שלה. לכן הנוהל נכתב לקובץ במקום להתבצע.

---

## מה אומת ומה לא — קרא לפני שאתה מתחיל

| | סטטוס |
|---|---|
| היעד `https://guralea.com/pilates.html#form` תקין | ✅ **אומת** — העוגן `id="form"` קיים ב-`pilates.html` שורה 121 |
| `tc-leads.co.il` מחזיר 404 | ⚠️ **לא אומת** — הפרוקסי של סביבת הענן חסם את הדומיין (403 בתעלה). מדווח על ידי לאה בלבד. **אמת את זה בעצמך בשלב 1** |
| המספר 168 | ⚠️ **לא אומת** — מדווח על ידי לאה. **ספור בעצמך בשלב 2** |
| הקישור מופיע כטקסט או ככרטיס מצורף | ❌ **לא ידוע — וזה הדבר הכי חשוב בקובץ הזה.** ראה שלב 3 |
| מזהה עמוד הפייסבוק של הסטודיו | ❌ **לא ידוע** — צריך לאתר בשלב 2 |

---

## ⚠️ החסם שעלול לבטל את כל המשימה — קרא את זה לפני הכל

**פייסבוק לא מאפשר להחליף קישור מצורף בפוסט שכבר פורסם.**

לפוסט עם קישור יש שתי צורות אפשריות:

**צורה א' — הקישור בגוף הטקסט בלבד**
```
בואי להתנסות בפילאטיס מכשירים ברמלה 💪
להרשמה: https://tc-leads.co.il/xxxxx
```
→ ✅ **ניתן לתיקון מלא.** עריכת הטקסט פותרת הכל.

**צורה ב' — כרטיס תצוגה מקדימה מצורף** (התמונה הגדולה עם הכותרת והדומיין, מתחת לטקסט)
→ ❌ **לא ניתן לתיקון.** אפשר לערוך את הטקסט, אבל **הכרטיס השבור יישאר מוצמד**. התוצאה: פוסט עם קישור תקין בטקסט ועדיין כרטיס שבור למטה — מצב שנראה מבולגן יותר מההתחלה.

**אם רוב הפוסטים הם צורה ב' — עצור, אל תמשיך, ודווח ללאה.** במקרה כזה האפשרויות היחידות הן למחוק ולפרסם מחדש (מאבד את כל הלייקים, התגובות והתאריך המקורי — **לא לעשות בלי אישור מפורש**, ולסיפורי קהילה זה אסור לגמרי לפי כלל 5), או להשאיר כמו שזה.

---

## שלב 1 — אימות מקדים (5 דקות)

לפני שנוגעים בכלום:

1. פתח בדפדפן את `https://tc-leads.co.il` ואת כתובת הקישור המלאה מתוך פוסט אמיתי. **אשר בעיניים שזה באמת 404.** אם זה עובד — עצור ודווח ללאה, אין משימה.
2. פתח `https://guralea.com/pilates.html#form` ואשר שהדף נטען **והעמוד קופץ לטופס**. צלם מסך.

---

## שלב 2 — בניית רשימת הפוסטים

**אל תגלול ידנית ב-Meta Business Suite.** 168 פוסטים זה בלתי אפשרי לסרוק בעין בלי לפספס.

השתמש ב-Graph API **לקריאה בלבד** (זו לא בניית מערכת פרסום חדשה — זו סריקה, ולפי הכלל של לאה סריקה וקריאה חופשיות):

1. פתח את [Graph API Explorer](https://developers.facebook.com/tools/explorer/), בחר את עמוד הסטודיו, וקח טוקן עם ההרשאות `pages_read_engagement` + `pages_manage_posts`.
2. משוך את כל הפוסטים עם עימוד:
   ```
   GET /{page-id}/posts?fields=id,message,created_time,permalink_url,attachments{type,url,unshimmed_url}&limit=100
   ```
3. סנן לפוסטים שבהם `tc-leads` מופיע ב-`message` **או** ב-`attachments`.
4. שמור לקובץ `posts-to-fix.json` — מזהה, תאריך, קישור קבוע, הטקסט המקורי המלא, וסוג הצירוף.

**שמור את הקובץ הזה לפני שאתה עורך משהו** (כלל 10 — כלום לא אובד). הטקסט המקורי הוא רשת הביטחון היחידה אם עריכה משתבשת.

**אמת את המספר.** אם יצאו 40 ולא 168, או 300 — עצור ודווח ללאה לפני שתמשיך.

---

## שלב 3 — ההכרעה: טקסט או כרטיס

מתוך `posts-to-fix.json`, ספור כמה מכל סוג לפי שדה `attachments.type`:

- `status` / `photo` / `video` עם הקישור רק ב-`message` → **צורה א'**, ניתן לתיקון
- `share` (כרטיס קישור) → **צורה ב'**, לא ניתן לתיקון מלא

**דווח ללאה את הפילוח לפני שאתה נוגע בפוסט כלשהו.** למשל: "מתוך 168 — 131 ניתנים לתיקון מלא, 37 עם כרטיס מצורף שיישאר שבור."

אם **רוב** הפוסטים הם צורה ב' — עצור לגמרי וחכה להחלטה שלה.

---

## שלב 4 — הפוסט הראשון, ועצירה לאישור

**לאה ביקשה במפורש לראות את הראשון שתוקן לפני שממשיכים לכולם. זו עצירה מוחלטת.**

1. בחר את הפוסט **הישן ביותר** מהרשימה (לא הפופולרי ביותר — אם משהו משתבש, שיהיה בפוסט עם הכי מעט חשיפה).
2. צלם מסך **לפני** — הפוסט כמו שגולש רואה אותו, לא מ-Business Suite.
3. ערוך: `⋯` → "ערוך פוסט" → החלף **רק** את כתובת ה-URL. **אל תיגע בשום מילה אחרת בטקסט.**
4. שמור.
5. פתח את הפוסט ב**כתובת הציבורית שלו, בחלון פרטי, לא מחובר** — ככה גולש רואה אותו (כלל 2: אימות דרך API לא נחשב).
6. צלם מסך **אחרי**.
7. **לחץ על הקישור בפועל** ואשר שהוא מגיע לטופס ב-pilates.html.
8. בדוק עברית — **סימן שאלה אחד = תיקון מיידי לפני כל דבר אחר** (כלל 3). עריכת פוסטים ישנים היא בדיוק המקום שבו קידוד נשבר.

**שלח ללאה: לפני, אחרי, ואישור שהקישור עובד. חכה ל"מאושר" מפורש. אל תמשיך בלי.**

---

## שלב 5 — 167 הנותרים

רק אחרי אישור מפורש של לאה.

**קצב — זה לא אופציונלי.** 168 עריכות ברצף מהיר בפייסבוק מסמנות את החשבון כאוטומטי ועלולות לגרום לחסימה זמנית של העמוד.

- **לכל היותר 20–25 עריכות בשעה**
- **הפסקה של 2–5 דקות בין עריכה לעריכה**, לא קצב קבוע
- **פרוס על 3–4 ימים.** לא הכל בישיבה אחת
- **אם פייסבוק מציג captcha, "נסה שוב מאוחר יותר", או חוסם עריכה — עצור לאותו יום לגמרי.** אל תנסה לעקוף

**אחרי כל עריכה:** סמן את הפוסט כבוצע ב-`posts-to-fix.json` עם חותמת זמן. אם הסשן נופל באמצע, הקובץ אומר בדיוק איפה עצרת.

**דגימת אימות:** כל 20 פוסטים, פתח אחד בחלון פרטי ואשר בעיניים. לא רק ב-API.

---

## שלב 6 — סיום ודיווח

1. אמת בעיניים **10 פוסטים אקראיים** מתוך המתוקנים, בחלון פרטי.
2. הרץ שוב את הסריקה משלב 2 ואשר ש-`tc-leads` לא מופיע יותר (פרט לצורה ב' שלא ניתנת לתיקון).
3. שמור את `posts-to-fix.json` הסופי בריפו או בדרייב (כלל 10).
4. דווח ללאה **בדוח 08:08** (כלל 7), לא באמצע היום:
   - כמה תוקנו
   - כמה לא ניתנו לתיקון ולמה
   - כמה נכשלו וצריכים סיבוב נוסף

---

## הצעה לשיקול לאה — לא לבצע בלי אישור

168 פוסטים זה עבודה של כמה ימים. סביר שרובם פוסטים ישנים כמעט בלי תנועה, ושהערך האמיתי מרוכז בקומץ קטן: הפוסט המוצמד, הפוסטים עם הכי הרבה חשיפה, וכל מה שמהשנה האחרונה.

**שווה להציע ללאה:** לתקן קודם את ה-20 המובילים בחשיפה (משיכת `insights` בסריקה משלב 2 נותנת את הדירוג), לראות שהכל עובד, ורק אז להחליט אם שאר 148 שווים את הזמן.

**זו הצעה בלבד.** לאה ביקשה 168 — אם היא לא אומרת אחרת, מתקנים 168.

---

## כללי העבודה שחלים כאן

מתוך `.claude/skills/standing-work-rules/SKILL.md`:

- **כלל 2** — "בוצע" רק אחרי שראית בעיניים בתצוגת גולש אמיתית. API לא נחשב אימות
- **כלל 3** — עברית: סימן שאלה אחד = תיקון מיידי לפני כל דבר אחר
- **כלל 5** — פוסטים אורגניים של סיפורי קהילה **לא נמחקים אף פעם**, מכל סיבה. אם פוסט מהרשימה הוא סיפור קהילה עם כרטיס שבור — הוא נשאר כמו שהוא
- **כלל 9** — תקן לפני שאתה מדווח. אם אי אפשר — שורה אחת: מה לא עובד ומה לאה צריכה לעשות
- **כלל 10** — כל טקסט נשמר בקובץ ברגע שהוא מוכן

---

## מה לעשות אם נתקעים

| מצב | פעולה |
|---|---|
| הקישור עובד, לא 404 | עצור. דווח ללאה. אין משימה |
| רוב הפוסטים עם כרטיס מצורף | עצור. דווח את הפילוח. חכה להחלטה |
| פייסבוק חוסם עריכה | עצור לאותו יום. אל תעקוף. חדש מחר |
| עברית נשברה בעריכה | תקן מיד, לפני כל פוסט נוסף (כלל 3) |
| פוסט מהרשימה הוא סיפור קהילה | אל תמחק. ערוך טקסט בלבד, או השאר (כלל 5) |
| המספר שונה מהותית מ-168 | עצור ודווח לפני שתמשיך |

---

## הערה על Graph API לעריכה בכמות

הגרסה המהירה היא `POST /{post-id}` עם `message=...` — הרבה פחות שביר מ-168 קליקים בדפדפן, ופחות סיכון לסימון כאוטומציה.

**אבל:** כלל 1 אומר שלא בונים שום מערכת חדשה בלי אישור מפורש של לאה, והתשתית המאושרת היא Metricool בלבד. **אל תלך לשם בלי לשאול אותה קודם במפורש.**

אם היא מאשרת — עדיין חלים כלל 2 (אימות בעיניים בחלון פרטי, לא דרך תגובת ה-API) וכלל 3 (בדיקת עברית אחרי כל שליחה). ה-API הוא קיצור דרך לביצוע, לא לאימות.
